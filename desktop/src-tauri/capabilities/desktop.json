{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop",
  "description": "Allow the bundled room UI to request one-use tickets from its owned Rust runtime.",
  "windows": ["main"],
  "local": true,
  "permissions": ["allow-runtime-ticket"]
}
