# WORKBOARD 작성·운영 규약

## 1. WORKBOARD의 역할

WORKBOARD는 현재 작업 상태와 권위 경로를 가리키는 짧은 라우터다.

다음 질문에 답할 수 있으면 충분하다.

1. 현재 활성 단계는 무엇인가.
2. 현재 활성 작업은 무엇인가.
3. 그 작업의 원본 owner는 어디인가.
4. 변경 종류에 따라 무엇을 읽어야 하는가.
5. 현재 단계를 종료하려면 어떤 실제 결과가 필요한가.

## 2. WORKBOARD 순수성

긴 작업 내용, 구현 이력, 상세 체크리스트, 검증 로그는 WORKBOARD 밖에서 소유한다.

WORKBOARD는 git history나 원본 작업 문서를 복제하지 않는다.

활성 작업은 가능한 한 포인터와 짧은 상태만 기록한다.

## 3. 문서 분할 규칙

상세 내용을 외부로 빼내기 위해 형식적인 문서를 생성하지 않는다.

다음 경우에만 별도 문서를 만든다.

1. 장기간 유지되는 계약과 자주 바뀌는 실행 상태를 분리해야 할 때.
2. 병렬 작업의 소유권과 경계를 명확히 해야 할 때.
3. 독립적으로 유지될 실제 계약이나 설계가 있을 때.

## 4. 상태 작성

현재 상태는 실제 전달 상태와 일치해야 한다.

`거의 완료`, `안정화 중`, `마무리 단계`처럼 종료 조건이 없는 표현을 피한다.

WORKBOARD의 한 줄 목적은 원본 제품 계약을 가리키는 포인터이지 두 번째 제품 계약이 아니다.

## 5. 처음 작성하는 절차

### 5.1 현재 권위 구조를 조사한다

제품, 아키텍처, 활성 작업, 검증과 저장소 규칙이 어디에서 소유되는지 확인한다.

파일 이름, 디렉터리 이름, 최신 수정 시각, 오래된 문서만 보고 권위를 추측하지 않는다.

### 5.2 중복과 충돌을 먼저 해결한다

같은 계약을 여러 문서가 정의하거나 문서와 실제 동작이 다르면 WORKBOARD가 임의로 하나를 선택해 정상 상태로 만들지 않는다.

충돌을 원본 소유자에서 다음과 같이 분류한다.

- 구현 결함
- 오래된 문서
- 아직 승인되지 않은 설계 변경
- 환경별 차이
- 검증 범위 부족
- unknown

### 5.3 현재 단계와 목적을 확인한다

계획된 모습이 아니라 실제 전달 상태를 기준으로 활성 단계를 하나 선택한다.

### 5.4 활성 작업의 owner를 확정한다

현재 작업의 문제, 범위, non-goals, acceptance와 verification을 누가 소유하는지 확인한다.

원본이 없더라도 독립적으로 오래 유지될 계약이 필요하지 않다면 새 문서를 만들지 않는다.

### 5.5 읽기 경로를 만든다

현재 권위만 등록하고 각 변경 유형을 가장 작은 필요한 문서·코드·owner 집합으로 연결한다.

모든 문서를 무조건 읽게 하지 않는다.

### 5.6 활성 작업은 포인터로만 기록한다

활성 작업의 상태와 원본 위치만 짧게 남긴다.

원본 문서의 세부 진행 항목이나 acceptance를 복사하지 않는다.

### 5.7 종료 조건을 검증한다

종료 조건은 실제 사용자 결과 또는 검증 가능한 시스템 상태여야 한다.

코드 작성, 테스트 추가, 문서 작성 자체를 제품 단계 종료로 취급하지 않는다.

### 5.8 전체 일관성을 확인한다

상단 상태, 활성 작업, owner, 읽기 경로와 실제 구현 상태가 같은 현재를 가리켜야 한다.

## 6. 갱신 순서

항상 다음 순서를 지킨다.

1. 사실을 소유하는 원본 코드·계약·작업 owner를 먼저 갱신한다.
2. 코드·런타임·검증에서 새 상태를 확인한다.
3. WORKBOARD의 상태, owner, 경로 또는 단계를 짧게 갱신한다.

WORKBOARD를 먼저 고쳐 새로운 사실을 선언한 뒤 구현을 맞추는 방식은 금지한다.

## 7. 품질 판정

좋은 WORKBOARD는 새 작업자가 짧게 읽고 다음을 바로 판단할 수 있게 한다.

- 현재 단계
- 현재 활성 작업
- 정확한 owner
- 자신의 변경에 필요한 최소 읽기 경로
- 현재 단계의 실제 종료 조건

검토 질문:

1. 긴 진행 기록이 들어가 있지 않은가?
2. 활성 작업 상세는 하나의 owner가 소유하는가?
3. WORKBOARD가 제품 계약이나 아키텍처를 다시 정의하고 있지 않은가?
4. 같은 사실을 여러 문서에서 함께 갱신해야 하지 않는가?
5. 정확히 하나의 단계만 활성 상태인가?
6. 활성 작업이 단계 종료 조건과 직접 연결되는가?
7. 각 변경 유형이 최소한의 권위 경로로 라우팅되는가?
8. 완료 상태가 실제 검증 범위를 넘어 확대되지 않았는가?
9. 모든 경로와 상태가 현재 유효한가?
10. 여러 커밋이 추가되어도 WORKBOARD 길이가 거의 그대로 유지되는가?

마지막 질문에 `아니오`라면 구현 이력이나 세부 작업이 WORKBOARD 안으로 들어온 것이다.

## 8. 대표적인 실패 형태

### 진행 일지화
완료 기능, 테스트, 수정 사항을 계속 추가한다.

### 원본 복제
활성 작업의 범위, acceptance, 진행 상태를 WORKBOARD에도 다시 적는다.

### 백로그화
미래 기능과 세부 순서를 길게 나열한다.

### 모든 문서 읽기 강제
변경 유형과 관계없이 전체 문서를 읽게 한다.

### 모호한 단계 상태
종료 기준 없는 상태 표현을 사용한다.

### 작업을 종료 조건으로 사용
코드, 테스트, 문서 작성을 완료했다는 사실만으로 단계를 끝낸다.

### WORKBOARD 선행 변경
검증되지 않은 상태를 WORKBOARD에 먼저 선언한다.

### 형식적 문서 증식
독립된 소유권이나 수명 차이가 없는데도 새 문서를 만든다.

## 9. 압축 원칙

> 원본 owner에서 계약과 상세 진행을 소유하고, 실제 결과를 검증한 뒤, WORKBOARD에는 현재 단계·활성 작업·권위 경로만 짧게 반영한다.
