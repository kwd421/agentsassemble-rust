# SDD

## 정의

AgentsAssemble은 큰 변경과 재구현에서 명세 주도 개발(Spec-Driven Development, SDD)을 따른다.

코드를 쓰기 전에 모든 구현 세부사항을 미리 결정하는 것이 아니라, 최종 계약·권위 경계·목적 아키텍처·수용 조건을 먼저 정한다. 내부 구현은 그 경계 안에서 좁은 프로덕션 수직 슬라이스를 구현하며 발견해도 된다.

## 워크플로우

1. **라우팅과 문제 정의**  
   WORKBOARD가 있으면 현재 작업의 owner와 원본만 읽는다. 사용자, 문제, 범위, non-goals, 관찰 가능한 완료 조건을 정한다. `빠르게`, `안전하게`, `잘` 같은 표현은 가능한 경우 관찰 가능한 기준으로 바꾼다.

2. **설계와 동결**  
   제품 계약은 무엇을·왜를 소유하고, 아키텍처는 어떻게를 소유한다. 권위 경계, 데이터 소유권, 실패 상태, 동시성 의미, 저장, 외부 프로토콜, 검증을 정한다. 되돌리기 비싼 계약과 경계만 동결한다.

3. **수직 슬라이스 구현**  
   최종 경계와 타입으로 실제 사용자 동작 하나를 끝까지 구현한다. 새 helper, type, rule, dependency, compatibility path를 만들기 전에 기존 owner와 현재 필요를 확인한다. 활성 슬라이스에 실제 동작이 없는 crate, 추상화, fallback owner를 만들지 않는다.

4. **검증과 종료**  
   acceptance에서 기대값을 역산해 핵심 경로, 현실적인 edge case, hard-stop을 검증한다. 테스트는 변경 자체가 아니라 중요한 실패 모드가 있을 때만 추가한다. 활성 슬라이스 안에서만 중복, 세레모니, 의존성 누수를 정리하고 실제 사용자 흐름을 다시 확인한다.

## 최소 명세

큰 변경에는 다음만 요구한다.

- 한 줄 정의
- 현재 계약
- non-goals
- acceptance criteria
- verification commands 또는 verification path

작은 변경은 다음으로 충분하다.

- 정의
- 현재 계약
- acceptance

별도 문서는 독립적인 지속 사실을 소유할 때만 만든다.
다른 문서의 계약을 복사하기 위한 문서를 만들지 않는다.

## AI 판단

판단에는 다음을 구분한다.

- 확인한 사실과 그 증거
- 적용 범위
- 불확실성
- 관찰한 증상
- 추론한 원인

확인하지 못한 clean, absence, compatibility, reachability를 승인하지 않는다.

아키텍처를 바꾸는 중요한 결정은 그 사실의 owner에 기록한다.
옛 Python 구현이나 오래된 문서는 자동으로 새 Rust 계약의 권위가 아니다.
Cutover가 완료된 기능은 Rust contract와 검증된 실제 사용자 흐름이 권위다.
