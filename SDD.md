# SDD

큰 변경과 재구현에 적용한다. 작은 변경은 요청과 diff에서 목적·계약·완료 조건이
명확하면 별도 명세를 만들지 않는다. 공통 구현·검증 규칙은 `Rule.md`를 따른다.

1. **계약과 완료 조건을 먼저 정한다.**
   `WORKBOARD.md`에서 현재 owner로 이동해 관련 부분만 읽는다. 목적, 현재 계약,
   non-goals, 관찰 가능한 acceptance, 검증 경로를 기존 owner에 기록한다.
2. **되돌리기 비싼 경계만 동결한다.**
   권위·데이터 소유권, 실패와 불확실성, 동시성, 저장·외부 프로토콜 중 변경에
   해당하는 경계를 정한다. 내부 구현 세부사항은 구현하며 결정해도 된다.
3. **실제 사용자 흐름을 끝까지 연결한다.**
   활성 단계의 의존 순서로 가장 작은 완결된 수직 슬라이스를 구현한다.
   acceptance를 검증하고 관련 실패를 수정한다. 첫 구현만으로 완료하거나
   구체적 근거 없이 이미 충족한 슬라이스를 계속 확장하지 않는다.
4. **증거와 상태를 원본에서 갱신한다.**
   확인한 사실·적용 범위·불확실성과 추론을 구분한다. 중요한 결정과 검증 결과는
   해당 owner에 기록하고 워크보드에는 상태와 링크만 반영한다. 별도 문서는
   독립적으로 유지할 계약이나 수명 차이가 있을 때만 만든다.

단계별 실행·리뷰·종료 조건은 `docs/PRODUCT_REIMPLEMENTATION_PLAN.md`가 소유한다.
