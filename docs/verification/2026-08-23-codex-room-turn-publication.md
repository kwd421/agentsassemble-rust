# Codex ordered room-turn publication verification — 2026-08-23

Scope: durable ordered-floor queueing and bounded assignment, provider-effect isolation from the room mutation owner, server-owned RoomPortal observation/publication, Codex poison recovery, and browser projection of live Agent Session state. This evidence uses owned Codex app-server and streamable-HTTP MCP fixtures; it is not a real-provider or Computer Use claim and does not cover the final Terra/Flash/Hy3 matrix.

## Automated evidence

- Post-review `make verify`: passed, including architecture and 800-line source gates, generated TypeScript, React production build and 29 frontend tests, Tauri checks and 12 tests, 60 persistence tests, 60 provider tests, 9 server unit tests, 15 server boundary/integration tests, warning-denied workspace Clippy, doc tests, and diff whitespace checks.
- Manual security review found that authenticated messages could grow a busy session's durable pending/inflight event-ID vectors without a hard aggregate cap and that restart merging used quadratic duplicate checks. The first fix capped new queueing, but follow-up review correctly rejected silently truncating pre-fix oversized authority during recovery. Lifecycle preparation and startup candidate loading now fail before provider effects on oversized, empty, or duplicate authority, while failure/stop/restart merges return an error rather than dropping later IDs. Focused tests cover atomic overflow rollback, canonical linear merge, and pre-effect lifecycle/reconciliation rejection.
- Source comparison and web review rejected direct use of ordinary provider final text as room authority. Ordered assignment now prepares a bounded canonical RoomPortal view and provider orientation separately. Completion requires the exact view read receipt and exactly one server-owned message publication or supported decline; ordinary Codex final text is ignored. Unknown handoff values become no handoff, while an exact known Agent Session ID remains structured routing authority.
- The persistence boundary commits a host `message_final`, one ordered target queue entry, and the first assignment together. Replay cannot issue a second assignment. A second message received while the provider is busy stays pending; completion atomically publishes one RoomPortal result, terminal turn state, provider-sync cursor, idle state, and the next assignment. Reusing the old turn ID fails closed.
- Direct target resolution covers all configured sessions before runtime eligibility, so a stopped or detached target keeps queued work unless kicked or muted. Agent-origin undirected messages prefer an eligible director. Backlogs move only the oldest complete prefix that fits 50 messages and the 20,000-character canonical view; tests cover 51 queued messages and two individually valid messages whose combination exceeds the character bound, proving the cursor never advances past deferred content.
- Failure coverage proves that an unknown provider failure becomes a stable public code, removes local paths and secret-shaped values, restores inflight input to pending, clears the active source/input authority, and marks the session recovery-required without assigning an error-state session.
- Corrupt provider-sync cursors, `busy` without an exact active/inflight tuple, incomplete active fields, and multiple active owners are rejected as inconsistent durable authority instead of being reinterpreted as an empty floor; the source-message transaction rolls back without an event.
- Routing regressions preserve the original final-direct-mention rule across unique session IDs, display names, split aliases, and bracketed mentions, while ambiguous aliases cannot select an Agent Session.
- The real TCP/WebSocket boundary creates and starts an Agent Session against an owned persistent Codex app-server fixture, blocks its first `turn/start`, and receives a second room-message ACK before releasing the provider. The fixture validates the exact `agentsassemble_room` MCP-tool elicitation response, reads its bearer only from the provider environment, uses that authenticated boundary for each private canonical room view and publication, and emits deliberately different ordinary assistant final text. The room publishes only the two portal messages, proving that the ordinary provider final is not room authority. Provider-process boundary scenarios are serialized by an explicit test mutex instead of depending on scheduler timing.
- A real bearer-authenticated rmcp client connects to the in-process capability-addressed loopback MCP, completes initialization and tool discovery, proves original-compatible publish-before-read staging remains non-durable until the same turn is read, and proves that a second terminal action is rejected. Separate HTTP probes prove that unauthenticated access returns 401, sixteen idle unauthenticated sockets remain under the hard eight-connection task bound and are evicted without blocking authenticated admission, the listener returns 404 outside the capability path, and a streaming request body beyond 64 KiB returns 413. Every connection is single-request and deadline-bound. No provider-spawned MCP child or RoomPortal authority file exists, and a command-construction regression proves the bearer never appears in argv.
- Fatal Codex turn poison is stopped once under the exact handle/owner and held until the durable failure checkpoint clears the matching runtime authority; a later call cannot reuse the poisoned driver or send a second turn. Attachment poison is explicitly restart-required. Startup adoption of an interrupted active turn requeues every inflight ID, clears the turn tuple, disables and detaches the session, and records explicit recovery instead of leaving a permanently busy session with no provider task.
- The browser accepts validated public `agent_session_state` events into the Agent Session projection, rejects events containing private runtime authority, and excludes `turn_started`, `turn_state`, and session-state coordination events from the visible message timeline.
- The workspace and Tauri shell passed `check` and warning-denied Clippy for `x86_64-pc-windows-gnu` using the installed rustup stable compiler and an isolated target directory.

## Authority and recovery boundaries

- SQLite is the only ordered-floor authority. Pending/inflight event IDs, active source event, provider-input cursor, and active turn ID are private durable fields; public session and turn events derive from them.
- The room task owns mutation order but not the provider wait. One owned child task performs provider I/O, and its result must re-enter the mutation owner before publication. Stop or replacement makes a late result stale, and a stale result cannot publish.
- Startup reconciliation and stop merge inflight input back into pending before clearing active authority. Successful or replayed start and stop paths attempt the recovered pending assignment without replacing their already committed ACK if later floor progression fails.
- Provider output and diagnostics remain untrusted. Only one bounded RoomPortal message or explicit decline enters the room; failures cross the shared redaction boundary.

## Cleanup and remaining scope

- Every mock provider process created by the tests was reaped by its adapter/server owner.
- The isolated Windows cross-check target directory and temporary Tauri Windows sidecar placeholder were removed after warning-denied workspace and desktop checks completed.
- No user-owned provider, browser, original-project, or desktop process was signalled or modified.
- Actual frontend Computer Use and real Agent Session verification remain pending until the exact Codex Terra, Antigravity Flash, and OpenCode Hy3 free matrix is implemented and available. No mock or automated browser result is counted as that evidence.
