# Rule

구현 시 적용한다. 제품 범위·승인·필수 게이트는 `AGENTS.md`가 소유한다.

1. **기존 소유자를 먼저 확인한다.**
   저장소 고유 사실은 코드·설정·실행 경로에서 확인한다. 새 메커니즘은 현재 제품 계약, 재현된 결함, 또는 측정된 비용·구체적 위협으로 정당화하고 기존 소유자나 더 작은 설계로 해결할 수 있는지 확인한다. 미래 확장, 관습, 편의만으로 추상화·상태·규칙을 추가하지 않는다. 정리는 요청 범위에서 직접 대체된 코드와 테스트에 한정한다.

2. **하나의 상태·계약에는 하나의 권위를 둔다.**
   스키마·정책·직렬화·검증·생성물은 소유자에서 파생하거나 불일치를 검사한다. Cache, timer, queue, derived flag는 권위 상태와 명시적 전이에서 파생한다. 재검증은 새 비신뢰 경계에서 기존 소유자가 보장하지 않는 조건에 한정하고, 암호 기술은 구체적 경계 요구에만 사용한다.

3. **실패를 숨기지 않는다.**
   누락·손상·미지원·실패·불확실성을 정상값이나 성공으로 바꾸지 않는다. Rust 실패의 Python 전환, provider 실패의 scraping/prompt 추론, 저장 실패의 in-memory success, invalid authority의 permissive default로 원인을 우회하지 않는다.

4. **수명과 동시성 의미를 소유시킨다.**
   설정·작업·외부 효과의 생성, 취소, 정리 주체를 명확히 한다. “먼저”, “한 번”, “완료”가 선택·시작·외부 효과·커밋 중 무엇인지 정하고 대기·병렬 실행·재시작에서도 보존한다. 숨은 전역 상태나 초기화 순서에 의존하지 않는다.

5. **주기 작업과 재시도에는 실제 계약이 있어야 한다.**
   외부 상태·연결 수명·불확실한 결과를 관찰할 필요가 있을 때만 사용한다. 주기, 상한, 종료 조건, 취소·정리 주체, 비용과 실패 노출을 확인한다. 누락된 통합·이벤트·동기화를 polling으로 덮지 않는다. 변경 리뷰는 영향받는 실행 경로와 결합된 기존 작업을 추적하고, 전체 단계 리뷰에서 저장소 전체의 주기 작업·재시도·실패 은폐를 확인한다.

6. **검증은 계약과 실패 모드에 맞춘다.**
   코드 변경만을 이유로 테스트를 추가하지 않으며 같은 계약·경로·사례를 중복 검사하지 않는다. 동시성 테스트는 임의 sleep이나 내부 상태 대신 barrier·event·통제 큐·fake clock으로 원인을 통제하고 공개 결과와 부작용을 확인한다. 각 구현 단위의 핵심 불변식은 기존 검증을 재사용할 수 있다. 영향받는 검증과 필수 게이트를 실행하고 관련 실패를 수정한다. 새 근거 없이 통과한 검증을 반복·확장하지 않는다. 검증 범위를 넘어 정상이라고 주장하지 않으며 미확인 결과는 `unknown`으로 남긴다.

7. **비용을 확인하고 필요한 최적화만 한다.**
   슬라이스에서는 영향받는 CPU·메모리·지연·작업/프로세스·디스크 비용을 확인하고, 전체 단계 검증에서 전체 자원 비용을 측정한다. 중요한 최적화는 구현 옆에 기존 비용·증상·위협, 의도와 소유 경계, 보존하는 제품·보안 불변식, 수용한 절충, 측정·검증 결과를 기록한다. 관측 근거 없이 성능·보안·확장성을 이유로 복잡성을 늘리지 않는다.

8. **파일 경계는 책임과 불변식을 따른다.**
   공통 메커니즘과 제품별 의미는 각각의 소유자에 둔다. 500줄부터 구조를 점검하고 800줄부터 강한 분리 후보로 본다. 1,000줄 초과는 금지하며 generated code·fixture·선언적 데이터의 예외는 구체적 근거와 게이트 변경 승인 절차를 따른다. 크기와 무관하게 독립된 도메인·권한·수명·불변식은 분리하되, 응집된 소유자를 줄 수 때문에 쪼개 상태 전달·공개 인터페이스·순환 의존·glue를 늘리지 않는다. Forwarding-only wrapper, 중복 변환, 공통 계층의 provider별 분기로 책임을 감추지 않는다.

9. **반복 위반만 필요한 경계에서 기계화한다.**
   diff에서 중복 계약·병렬 권위·fallback·소유자 없는 작업·공개 표면의 증가를 확인한다. 반복되는 실제 위반은 기존 lint·test·CI 소유자로 막는다. 새 차단 게이트의 승인 조건은 `AGENTS.md`를 따른다.
